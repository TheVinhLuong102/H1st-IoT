Django >= 1.11.14, <2   # Django2 bug in Py3: 'Options' object has no attribute 'virtual_fields': https://github.com/klen/mixer/issues/81

Django-AutoComplete-Light >= 3.2.10
Django-Generic-M2M >= 0.3.1
Django-GM2M >= 0.6
Django-QuerySetSequence >= 0.8
Django-Tagging >= 0.4.6
Django-Taggit >= 0.22.2

Django-Debug-Toolbar >= 1.9.1
Django-Grappelli >= 2.11.1

Django-Extensions >= 1.9.9, < 2

Django-Nested-InLines >= 0.1

# Django REST Framework
DjangoRESTFramework >= 3.8.2
Markdown >= 2.6.11   # Markdown support for the browsable API.
Django-Filter >= 2.0.0   # Filtering support

SQLAlchemy >= 1.2.10
Alembic >= 0.9.10
SQLAlchemy-Migrate >= 0.11.0

PsycoPG2-binary >= 2.7.5

AWSEBCLI >= 3.14.3

AWSCLI >= 1.14.51
Boto3 >= 1.6.4

PyYAML >= 3.13
Ruamel.YAML >= 0.15.43

Tornado == 4.5.1   # *** later ver breaks Jupyter (SSL err) ***
